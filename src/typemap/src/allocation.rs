use cage::get_cage;
use cage::memory::mem_helper::*;

/// This function is ued to allocate a memory region in grate in order to pass arguments into 
pub allocate_str(path_arg: u64, path_arg_cageid: u64, cageid: u64) -> u64 {

}